[project]
name = ${project_name}

[build]
# make = Unix Makefiles
# macro = MACRO1,MACRO2
# target = Debug | Release

[test]
include = $$ENV{GTEST_HOME}/include
link_path = $$ENV{GTEST_HOME}/lib
lib = gtest
exclude = None

[depends]
# dependent packages